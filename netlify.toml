[build]
  command = "npm run build"
  publish = "dist/registro-proveedor-web/browser"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["express"]
  included_files = ["agent/prompt.md", "fixtures/**"]
